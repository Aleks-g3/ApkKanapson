<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Kanapson.MainPage">

    <StackLayout Padding="5,0,0,0" Margin="50,100,50,0">
        <Label Text=""/>
        <Entry x:Name="username" Placeholder="Username"/>
        <Entry x:Name="password" Placeholder="Password"/>
        <Label x:Name="messege"/>
        <Button Text="Login" Clicked="Button_Clicked"/>
        <Label Margin="180,0,0,0" BackgroundColor="White">
            <Label.FormattedText>
                <FormattedString>

                    <Span Text="Zarejestruj się"
                  TextColor="Blue"
                  TextDecorations="Underline">
                        <Span.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding TapCommand}"
                                          CommandParameter="https://docs.microsoft.com/xamarin/" />
                        </Span.GestureRecognizers>
                    </Span>
                </FormattedString>
            </Label.FormattedText>
        </Label>

    </StackLayout>

</ContentPage>
